# 参与贡献

**简体中文** | [English](CONTRIBUTING.md)

改动如何进入本项目：提交到哪里、分支如何命名、提交信息怎么写、审核关注哪些内容。
项目本身以及构建、运行方法见 [README](README.zh-CN.md)。

## 参与方式

- **报告问题或提出需求**：新建 [issue](https://github.com/D-Robotics/gs130_camera_sdk/issues)，
  说明观察到的现象或需要的能力，并给出开发板型号、相机型号、执行的命令及其输出。
- **修改代码或文档**：提交 pull request 即可。不需要先开 issue，也不需要事先沟通。

## 改动提交到哪个分支

改动进入 `develop`，不进入 `main`。`main` 只存放已发布的版本。

1. Fork 本仓库。已有写权限的，直接在仓库内新建分支，无需 fork。
2. 从 `develop` 新建分支，命名为 `feature/<主题>`，例如
   `feature/eeprom-new-vendor`、`feature/fix-imu-timestamp`。
3. 在分支上提交。可以分多次提交，提 PR 前把提交整理干净。
4. 推送该分支，向 **`develop`** 提交 pull request。

不要直接推送到 `develop` 或 `main`，也不要自行合并自己的 pull request。

## 提交信息

提交信息是给以后查历史的人看的，不是写给审核者的报告。

- **标题**：一句话说明这次改动做了什么，用祈使句，72 字符以内，结尾不加句号。
  写 `Add ...`、`Fix ...`、`Clarify ...`，不要写 `Added`、`Fixes bug`。
- **正文**：说明改了什么、为什么这样改。一般三到八行即可，标题已说清时可以不写。
- **只写事实，不写辩解**：避免出现「没有臆造缺失项」「而不是返回部分结果」这类表述。
- **细节放到该放的地方**：偏移量、字段布局、校验公式、命令行与期望值属于代码注释和文档，
  不要写进提交信息。
- **不要复述 diff**：改动了哪些文件、增删多少行，`git show --stat` 已经写明。
- **双语欢迎但不强制**：写两种语言时英文在前，每种语言写一次即可，
  不要一段英文紧跟其中译。
- 改写过已经推送的分支时，使用 `git push --force-with-lease`，不要用裸 `--force`。

## 审核关注什么

- 一个 PR 只做一件事，PR 描述中说明这件事是什么。
- 不包含无关改动。顺手重排格式、重命名本次不需要改动的文件，会让审核变难，会被要求拆出去。
- 不包含构建产物。`.gitignore` 已覆盖 `core/{build,out}`、
  `python/{build,dist,*.egg-info}`、`__pycache__`、`ros2/{build,install,log}`。
- 结论与证据相符。写了「已在硬件上验证」，就意味着确实有人在硬件上执行过，
  且 PR 中说明了执行方式；在主机上测的就写主机上测的。
  没有验证的就写没有验证，不要暗示它可用。
- 代码改动伴随对应的文档和注释更新。

## 提交 PR 前运行哪些检查

按改动内容执行对应检查。先从干净的工作区构建一次，避免结果来自上一次构建。

```bash
cd core
make -j$(nproc)            # 库、示例、工具、Debian 包
make lib                   # 只构建库

# 运行与改动相关的部分，例如
out/RDKX5/gs130 detect eeprom -b 4 -a 0x50
out/RDKX5/gs130 shell -d GS130WI
```

Python 封装的硬件测试在 `python/` 下运行：

```bash
python3 test/test_gs130.py GS130WI rect 544 448 30 200
```

未接硬件时该测试**跳过**而不失败，因此没有开发板时的「通过」说明不了什么。
请写明运行的是哪一种情况。

ROS 2 封装：

```bash
cd ros2 && colcon build --packages-select gs130_camera
```

## 改动本身的要求

- 跟随周围代码的风格。项目没有统一的格式化工具，沿用所改文件已有的风格。
- 代码改动伴随对应的文档和注释更新。标定字段布局、校验公式这类细节属于代码注释和文档，
  不要写进提交信息或 PR 描述。
- 不要提交构建产物。
- 不要重命名、重排格式或 `chown` 本次不需要改动的文件。提交前先查看 `git status`。
- 只改注释、格式或文档的改动，代码本身必须完全不变。
