<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>gs130_ros</name>
  <version>0.0.1</version>
  <description>
    Publishes the GS130 stereo camera and IMU as standard ROS 2 messages by
    calling the SDK's C API directly.  Needs libgs130 and gs130.h, both of
    which come from the gs130_sdk repository; neither is a rosdep key, so the
    build looks for them on the system and in the checkout.
  </description>
  <maintainer email="hachi-leaf@users.noreply.github.com">hachi-leaf</maintainer>
  <license>MIT</license>
  <url type="repository">https://github.com/hachi-leaf/gs130_sdk</url>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>builtin_interfaces</depend>
  <depend>tf2_ros</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
